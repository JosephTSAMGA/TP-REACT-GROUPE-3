# Frontend

```bash
npm install
npm run dev
```

Arborescence : `src/app`, `src/features/{game,map,result}`, `src/shared`.  
Mocks activés par défaut (`VITE_USE_MOCKS` ≠ `false`).

Carte = Leaflet + OSM (P2), pas une carte maison. Photos = URLs backend, pas `public/`.  
Voir `docs/CONSIGNES.md`.
