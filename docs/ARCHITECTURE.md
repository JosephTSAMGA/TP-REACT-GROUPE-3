# Architecture

App React unique dans `frontend/` (GetClose). P4 hors projet.

Voir [CONSIGNES.md](./CONSIGNES.md).

Scoring Haversine **côté client**. API orale = Nominatim. Historique = localStorage.
